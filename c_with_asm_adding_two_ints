#include <stdio.h>


int main()
{
  int a = 1;
  int b = 2;
  int sum = 0;

  asm(
      "mov %1, %%eax \n"
      "mov %2, %%ebx \n"
      "add %%ebx, %%eax \n"
      "mov %%eax, %0 \n"
 
      : "=r"(sum)
      : "r"(a),"r"(b)
      : "%eax", "%ebx"
     );

  printf("%d\n", sum);

}
